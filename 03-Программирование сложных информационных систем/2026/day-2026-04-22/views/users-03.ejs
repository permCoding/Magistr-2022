<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <title>Users</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="shortcut icon" href="/css/favicon.ico" />
    <style>
        .indent-div {
            margin-left: 150px;   /* Сдвигаем весь блок вправо на 50px */
            width: 400px;
        }
    </style>
</head>

<body>
    <div class="indent-div">
        <h3> <%= title %> </h3>
        <% arrayUsers.forEach( user => { %>
            <li>
                <%= user.lastName %> 
                <%= user.firstName %> 
                <%= user.rating %>
            </li>
        <% }); %>    
    </div>
    <!-- 
        1) заменить forEach на цикл for по индексам массива arrrayUsers
        2) добавить нумерацию по порядку от 1 и до последнего
        3) разместить записи не в тег li, а в таблицу 
            - каждая запись в отдельной строке таблицы
            - каждое поле в отдельной ячейке
            - сверху - заголовки полей
        4) добавить функцию сортировки
            - при клике по заголовку сортируется по возрастанию
            - по тому столбцу, по которому кликнул пользователь
            - кроме столбца с нумерацией - он всегда от 1 
        5) добавить структурный оператор if - НЕ тернарную операцию
            - добавить столбец справа
            - в котором писать: принят или не принят
            - если рейтинг более 190, то принят
            - иначе - не принят 
            - принят - зелёным цветом, не приянт - красным
    -->
</body>
</html>